<p id="notice"><%= notice %></p>

<h1>Feedbacks</h1>

<table class="table table-dark">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Comments</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @feedbacks.each do |feedback| %>
      <tr>
        <td><%= feedback.name %></td>
        <td><%= feedback.email %></td>
        <td><%= feedback.comments %></td>
        <td><%= link_to 'Show', feedback , :class => "btn btn-warning"%></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Feedback', new_feedback_path %>
